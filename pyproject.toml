[project]
name = "supabase-admin-mcp"
version = "0.2.1"
description = "MCP server for self-hosted Supabase — 47 tools, REST API only, no DATABASE_URL"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0", "httpx>=0.28"]
